This holds python scripts that proccesses images for use in Structure from motion libaries

Currently it converts 360 images (panos) to rectilinear images

Example usage:

python main.py /input_path /output_path